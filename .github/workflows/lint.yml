name: Lint CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Install shellcheck, mandoc
      run: sudo apt-get install -y shellcheck mandoc

    - name: Check src scripts for errors
      run: cd src && make lint

    - name: Check man pages for errors
      run: cd man && make lint

# End of file.
